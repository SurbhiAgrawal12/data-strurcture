/*
problem Description: We have N disks(each of equal radius).Every disk has assigned a distinct number from 1 to N.
Disks are placed one over other in a single pile.
we want to sort this pile of disk in increasing order from top to bottom in minimum number of steps?
*/

let stack = [7, 3, 6, 9, 1, 4, 8];

